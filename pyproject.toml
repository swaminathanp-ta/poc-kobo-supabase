[project]
name = "bvl-player-registration"
version = "0.1.0"
description = "Supabase setup and dashboard tools for the BVL registration PWA"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
]
